# Walmart — predicting weekly sales with a (regularised) linear regression
# Install with:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# --- Notebook & data manipulation ---
pandas==3.0.5
numpy==2.5.2
ipykernel==7.3.0            # Jupyter kernel for walmart_project.ipynb

# --- Modelling ---
scikit-learn==1.9.0         # ColumnTransformer / Pipeline, LinearRegression, Ridge, Lasso,
                            # GridSearchCV. root_mean_squared_error needs >= 1.4.
scipy==1.18.1               # stats.ttest_ind for the holiday-flag test in section 3.3.
                            # Already a scikit-learn dependency; pinned here because
                            # the notebook imports it directly.

# --- Visualisation ---
plotly==7.0.0               # every chart in the notebook
kaleido==1.3.0              # renders the figures as static PNG, both inline (so the notebook
                            # stays readable on GitHub, which strips interactive plotly output)
                            # and into images/ for the README and the oral presentation.
                            # Needs a Chrome:  .venv/bin/plotly_get_chrome
nbformat==5.11.1            # required by plotly to render figures inside the notebook

# --- Running the notebook headlessly ---
nbclient==0.11.0            # only needed to execute the notebook from the command line;
                            # not used by any cell.
